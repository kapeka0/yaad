{
  "name": "yaad",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "docker compose up --build",
    "db:generate": "pnpm --filter @yaad/db db:generate",
    "db:migrate": "pnpm --filter @yaad/db db:migrate"
  },
  "devDependencies": {
    "postgres": "^3.4.8",
    "typescript": "^5.4.5"
  }
}
